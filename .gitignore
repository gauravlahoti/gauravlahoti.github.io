.DS_Store
node_modules/
.env
.env.local
*.log
.idea/
.vscode/

# Editor/OS artifacts
*.swp
*.bak
*~

# Build output (none today, but reserved so they never sneak in)
dist/
build/
coverage/
_site/

# Cloudflare wrangler local cache (also covered by backend/.gitignore but
# pinning at root in case you ever run wrangler from a different folder).
.wrangler/

# Source PDFs kept locally for reference; the public resume lives in assets/img/.
/Profile.pdf
/Gaurav Lahoti Senior Architect Resume.pdf

# Claude Code internal tooling.
.claude/*.lock
.claude/scheduled_tasks.lock

# Agent working memory: local notes, not project source.
# Untracked deliberately — security-cost-auditor/ holds an endpoint-to-auth map
# and an audit findings list, and this repo is public.
.claude/agent-memory/
