{
  "name": "resume-gate-local",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Local SQLite-backed resume gate. Mirrors the Cloudflare Worker in src/index.js.",
  "main": "local-server.js",
  "scripts": {
    "start": "node local-server.js",
    "leads": "sqlite3 leads.db \"SELECT name, email, datetime(downloaded_at,'unixepoch') AS at FROM resume_downloads ORDER BY downloaded_at DESC LIMIT 50\""
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "better-sqlite3": "^11.5.0",
    "jose": "^6.2.3"
  }
}
